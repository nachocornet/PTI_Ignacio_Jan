# DB

Capa de datos SQLAlchemy.

## Archivos

- `database.py`: engine, session y Base.
- `models.py`: modelos `AuthSession` y `CitizenDB`.

## Persistencia

Por defecto usa `deployments/runtime/ssi_sessions.db`.
